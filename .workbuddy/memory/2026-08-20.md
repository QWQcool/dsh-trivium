# 2026-08-20

## dsh-trivium 推广文章（腾讯云开发社区）
- 为 dsh-trivium 0.3.0 插件撰写社区推广文，产出 `article/dsh-trivium-promo.md`（重构后的中文稿）+ 4 张 SVG 配图：`article/figures/{arch,loop,graph,injection}.svg`。
- 改写策略：保留用户「机制可以比；分数不要编」的诚实口径；把原稿防御性的「限制」段改写成「我们实测了什么 / 没实测什么」信任段；结构按 痛点 → 不另起服务 → 跨会话回路 → 图模型(边一等公民) → 四工具 → 默认少注入 → 人能改错 → 诚实边界 → 安装 展开。
- 文末附"发布建议（发布前删除）"：标题方案、标签、配图、长度、SVG 转 PNG 提示。
- v2：按用户要求压短至约 1700 字（合并小节、删冗余 prose），并新增第 7 节「实操：和 WorkBuddy MEMORY.md 互导」（双向：WorkBuddy→dsh-trivium 严导入 / dsh-trivium→WorkBuddy Markdown 投影），强调单向迁移非常持续同步、限写 ≤3000字/24条。
