# dsh-trivium — host-plane memory kernel for every preset.
- insert:
    - id: dsh-trivium
      name: dsh-trivium
      config:
        autoRecall: false
        extractEnabled: true
        writeApproval: false
        mapTokenBudget: 400
        expandDepth: 1
        topK: 8
